Ryan Clarke's bespoke dotfiles
========

Source dotfiles' `.bash_profile` in `~/.bash_profile`
>`. install.sh`

Setup git configuration to dotfiles default
>`. git/setupconfig.sh`

Setup vim configuration
>`. vim/setup.sh`
